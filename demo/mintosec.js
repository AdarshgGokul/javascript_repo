var mins = parseInt(prompt("Enter no of minutes"));
function convertMinutesToSeconds(m){
    document.write(m*60);
}
convertMinutesToSeconds(mins);

var num1= parseInt(prompt("Enter first number"));
var num2= parseInt(prompt("Enter second number"));
var total=0;
function sum(a,b){

    return a+b;
}
total=sum(num1,num2)